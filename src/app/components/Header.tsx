import HeaderText from "./HeaderText"

export default function Header() {
    return (
        <header className="bg-green w-full min-h-screen pt-30 flex justify-center items-center">
            <HeaderText />

        </header>
    )
}